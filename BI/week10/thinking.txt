Thinking "如果你是某P2P租车的技术负责人，你会如何设计个性化推荐和搜索排序阐述相似车型，搜索排序的设计方法可能的embedding策略"

	根据车型价格距离等因素将特征将每个用户的点击行为按一定规则建立成一个序列，序列中元素为每个租车车型，通过学习出每个车型的listing利用
	skip-garm类似的技术embedding。每个车型当成一个word，得到美国车型的向量，而且可以计算余弦相似度，并利用相似度得出排序或者搜索结果。
